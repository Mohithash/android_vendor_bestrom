# BestROM board-level defaults (include from BoardConfig if needed)
# e.g. AVB, reserved partition sizes — device tree usually owns these.

# Example reserved size hook when using voltage-like boards:
# -include vendor/voltage/config/BoardConfigReservedSize.mk
